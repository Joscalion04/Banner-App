<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="#FFFFFF"
    android:layout_margin="8dp"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Course Name - Highlighted -->
        <TextView
            android:id="@+id/tvNombreCurso"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/nombre_del_curso_item"
            android:textColor="#81130A"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp"/>

        <!-- Course Code -->
        <TextView
            android:id="@+id/tvCodigoCurso"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/c_digo_xxx"
            android:textColor="#212121"
            android:textSize="14sp"
            android:layout_marginBottom="4dp"/>

        <!-- Course Details -->
        <TextView
            android:id="@+id/tvDetallesCurso"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/_3_cr_ditos_4_hrs_semana"
            android:textColor="#616161"
            android:textSize="13sp"
            android:layout_marginBottom="12dp"/>

        <!-- Career Spinner - Styled -->
        <Spinner
            android:id="@+id/tvSpinnerCarreraCurso"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"        android:spinnerMode="dropdown"
            android:focusable="false"
            android:clickable="false"
            android:focusableInTouchMode="false"
            android:enabled="false"
            android:layout_marginTop="8dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"/>

        <!-- Cycle Spinner - Styled -->
        <Spinner
            android:id="@+id/tvSpinnerCicloCurso"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"        android:spinnerMode="dropdown"
            android:focusable="false"
            android:clickable="false"
            android:focusableInTouchMode="false"
            android:enabled="false"
            android:layout_marginTop="8dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"/>

    </LinearLayout>
</androidx.cardview.widget.CardView>