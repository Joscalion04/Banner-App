<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="#FFFFFF"
    android:layout_margin="8dp"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- ID Card - Highlighted -->
        <TextView
            android:id="@+id/tvCedulaProfesor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/cedulaProfesor"
            android:textColor="#E0281A"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginBottom="6dp"
            android:drawableStart="@android:drawable/ic_menu_my_calendar"
            android:drawablePadding="8dp"
            android:drawableTint="#E0281A"/>

        <!-- Name -->
        <TextView
            android:id="@+id/tvNombreProfesor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/nombreProfesor"
            android:textColor="#212121"
            android:textSize="15sp"
            android:layout_marginBottom="4dp"
            android:drawableStart="@android:drawable/ic_menu_edit"
            android:drawablePadding="8dp"
            android:drawableTint="#E0281A"/>

        <!-- Phone -->
        <TextView
            android:id="@+id/tvTelefonoProfesor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/telefonoProfesor"
            android:textColor="#424242"
            android:textSize="14sp"
            android:layout_marginBottom="4dp"
            android:drawableStart="@android:drawable/ic_menu_call"
            android:drawablePadding="8dp"
            android:drawableTint="#E0281A"/>

        <!-- Email -->
        <TextView
            android:id="@+id/tvEmailProfesor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/emailProfesor"
            android:textColor="#424242"
            android:textSize="14sp"
            android:drawableStart="@android:drawable/ic_dialog_email"
            android:drawablePadding="8dp"
            android:drawableTint="#E0281A"/>

    </LinearLayout>
</androidx.cardview.widget.CardView>