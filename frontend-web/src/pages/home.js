// src/pages/Home.js

const Home = () => (
    <div style={{ padding: '2rem' }}>
        <h2>Home - Página Pública</h2>
        <p>Bienvenido a la demo de Login con React Router y fetch simulado.</p>
    </div>
);

export default Home;
