<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="@android:color/white">

    <!-- Título con estilo, tamaño y color -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gestión Académica"
        android:textSize="28sp"
        android:textColor="#6200EE"
        android:textStyle="bold"
        android:layout_marginBottom="20dp"
        android:padding="10dp"/>

    <!-- Descripción con estilo, tamaño de fuente y espaciado -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="¡Bienvenido a la plataforma de gestión académica!"
        android:textSize="18sp"
        android:textColor="#333333"
        android:layout_marginBottom="30dp"
        android:gravity="center"
        android:layout_marginTop="10dp" />

    <!-- Botón de acción, como si fuera un "Comenzar" -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Comenzar"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:background="@color/black"
        android:paddingHorizontal="20dp"
        android:paddingVertical="10dp"
        android:layout_marginTop="20dp"
        android:layout_gravity="center"/>

</LinearLayout>

